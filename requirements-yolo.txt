# ─────────────────────────────────────────────────────────────
# Dependencias opcionales para el pipeline de visión (Fase 2)
# Instalar con:  pip install -r requirements-yolo.txt
#
# Para CPU, instalar primero (pip puede seleccionar CUDA desde PyPI):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# Para GPU con CUDA 12.1, instalar torch manualmente ANTES:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# Para GPU con CUDA 11.8:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# Extras SOLO para optimización de modelos (docs/YOLO_FINETUNING.md §6),
# no necesarios para correr el servidor:
#   pip install onnx onnxruntime    # export/inferencia ONNX
#   pip install openvino            # cuantización INT8 en CPU Intel
# ─────────────────────────────────────────────────────────────

ultralytics==8.4.0  # YOLO26 compatibility target; real-weight smoke in CI

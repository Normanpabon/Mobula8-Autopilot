# ─────────────────────────────────────────────────────────────
# Mobula8 Autopilot — dependencias del servidor
# Instalar con:  pip install -r requirements.txt
# Ver SETUP.md para instrucciones de entorno conda.
# ─────────────────────────────────────────────────────────────

# ── Servidor web y telemetría ─────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0     # incluye websockets y httptools
pyserial>=3.5
pydantic>=2.0

# ── Video — captura USB y WebRTC ──────────────────────────────
numpy>=1.26.0
opencv-python>=4.9.0
aiortc>=1.9.0
av>=12.0.0                    # PyAV — manejo de frames de video
